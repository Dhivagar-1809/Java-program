class Solution {
    public int maximumWealth(int[][] accounts) {
        int max=0;
        for(int arr[]:accounts){
            int sum=0;
            for(int x:arr){
                sum+=x;
            }
            if(max<sum) max=sum;
        }
        return max;
    }
    
}
